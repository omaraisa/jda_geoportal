services:
  geoportal1:
    build:
      context: ./jda_geoportal
    environment:
      - PORT=3000
    expose:
      - "3000"
    networks:
      - gis_net

  geoportal2:
    build:
      context: ./jda_geoportal
    environment:
      - PORT=3000
    expose:
      - "3000"
    networks:
      - gis_net

networks:
  gis_net:
    external: true
